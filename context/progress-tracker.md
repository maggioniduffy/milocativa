# Progress Tracker

Update this file whenever the current phase, active feature, or implementation state changes.

## Current Phase

Public site foundation — landing, catalog, and building detail pages implemented with placeholder data. Clerk auth (sign-in/sign-up, role-based redirect, role provisioning) is now wired end to end.

## Current Goal

Wire the catalog and building pages to Supabase (real listings, buildings, and unit statuses).

## Completed

- Landing (home) page at `/` inside the `app/(public)` route group, implemented from the v0 design spec (v0.app chat `jGFCPmKt5rE`), fully tokenized and in Spanish:
  - `components/layout/`: `navbar.tsx` (sticky, scroll elevation, mobile sheet), `siteFooter.tsx`, `brandMark.tsx`.
  - `components/home/`: `hero.tsx`, `aboutSection.tsx`, `catalogPreview.tsx`, `categoryCard.tsx`, `listingCard.tsx`, `categoryBadge.tsx`, `howItWorks.tsx`.
  - `components/motion/reveal.tsx`: shared scroll-into-view fade/slide wrapper (Framer Motion, respects `prefers-reduced-motion`).
  - `content/site.ts` (brand, nav, footer, metadata) and `content/home.ts` (hero, about, catalog preview, how-it-works) hold all Spanish copy; sample listings in `content/home.ts` are placeholders until Supabase powers the catalog.
  - `types/domain.ts`: `Category`, `RentalStatus`, `CATEGORY_COLORS`, `RENTAL_STATUS_COLORS` per `ui-context.md`.
  - `app/(public)/layout.tsx` wraps public pages with navbar + footer; root layout metadata now comes from `content/site.ts`.
  - shadcn/ui `card`, `badge`, `sheet` added via the CLI (unmodified); AI-generated placeholder photos in `public/images/`.
- `npm run build` passes; all routes static.
- Catalog page at `/catalogo` and building detail at `/edificios/[slug]`, implemented from the Claude Design handoff (`context/designs/Milocativa landing page/design_handoff_milocativa/`):
  - `content/catalog.ts` and `content/building.ts` hold all Spanish copy plus placeholder buildings/products/units until Supabase powers the catalog.
  - `types/domain.ts` extended: `Category` now includes `parking`; added `UnitStatus` (`RentalStatus | available | external`), `UNIT_STATUS_COLORS` (with borders), `CATALOG_FILTER_COLORS`, and the external/parking hatch gradients.
  - `components/catalog/`: `catalogSearch.tsx` (expanded search + type/price/duración/disponibilidad filter row + compact navbar pill), `buildingsRail.tsx` (snap carousel with arrows, favorites, staggered "Mostrar todo" card), `productsGrid.tsx` + `productCard.tsx` (Airbnb-style cards, category dot badges, favorites, "Con cochera disponible" badge).
  - `components/building/`: `buildingDiagram.tsx` (interactive floor/unit cross-section with hover/tap popovers, inert hatched "No publicado" units, striped parking level, staggered riseIn entrance respecting `prefers-reduced-motion`), `statusLegend.tsx`, `buildingSummary.tsx` (sticky summary + CTA).
  - Navbar (`components/layout/navbar.tsx`) hosts the catalog search on `/catalogo`: past 40px scroll the search block collapses and a compact pill replaces the center links; clicking it scrolls back to top.
  - Type filter state lives in the `?categoria=` URL param (`hooks/useCatalogFilter.ts`), so the landing/footer links (`/catalogo?categoria=estate|machinery|service`) filter the grid directly; `useSearchParams` consumers are Suspense-wrapped so both routes stay static.
  - Catalog products reuse landing photos where they match; remaining covers are CSS gradient placeholders per the handoff (replace with real photos).
  - `context/designs/**` excluded from ESLint (prototype runtime files are reference-only).
- Landing reworked to match the Claude Design handoff (replacing the v0 look):
  - Navbar transparent at top → solid white + border + shadow past 24px scroll (all pages); brand mark now uses the petrol building-glyph tile.
  - Hero: radial petrol blob + SVG-grain overlay, pill badge with map pin, pill CTAs with arrow, dot-separated stat row, photo panel with deep shadow, staggered reveals.
  - "Nosotros" is a 3D `rotateX` hover/tap flip card (`aboutSection.tsx`); title accent uses Instrument Serif italic (`--font-serif-accent`, loaded via `next/font`); reduced motion crossfades instead of rotating.
  - Category cards use the icon-tile + title top-row layout; catalog preview cards are Airbnb-style (`listingCard.tsx`: dot badge, heart, star rating, price + availability status with success/warning tone) — new copy and six handoff listings in `content/home.ts`.
  - "Cómo funciona" lives in a large white rounded panel with a corner blob; footer got the rounded-top-corners + soft top shadow treatment.
  - `topo-bg.js` ported to `components/layout/topoBackground.tsx` (fixed animated topographic canvas behind landing + building pages; static frame under reduced motion).
  - `components/home/categoryBadge.tsx` removed (superseded by the inline dot badge).
- Product detail page at `/catalogo/[id]`, Airbnb-style single-listing layout, placeholder data:
  - `content/product.ts`: `ProductDetail` (description, quick facts, features, gallery photos) per catalog product id, joined to `CatalogProduct` via `getProductById`.
  - `components/product/`: `productGallery.tsx` (bento hero + up to 4 tiles, degrades to single hero photo or plain cover for products without extra photos), `productOverview.tsx` (quick facts row, description, features checklist, location note), `productBookingCard.tsx` (sticky price/rating card, local favorite toggle, "Iniciar conversación" CTA linking to `/sign-in`).
  - `productCard.tsx` now links to `/catalogo/[id]` instead of `#`.
  - No live chat/booking wired yet — CTA only routes to sign-in, matching the "auth flows not started" phase.
- Clerk auth (`/sign-in`, `/sign-up`), per `context/feature-specs/01-auth-page.md`:
  - Fixed a pre-existing bug: `proxy.ts` lived at the repo root, but with a `src/` layout Next.js only loads it from `src/proxy.ts` — Clerk's own middleware was silently never running (`auth()`/`currentUser()` threw). Moved it to `src/proxy.ts`; `/admin` gating (`auth.protect({ role: "admin" })`) now actually executes. No other change to its logic.
  - `src/app/layout.tsx` wraps the app in `<ClerkProvider>` with a shared `appearance` (`lib/clerk/appearance.ts`, tokenized via CSS vars — no hardcoded hex) and `localization` (`content/auth.ts` — full Spanish override of Clerk's default English copy for the OAuth/OTP/password flows actually in use). `localization`/global `appearance` are ClerkProvider-only props in this Clerk version (7.x) — `<SignIn/>`/`<SignUp/>` no longer take a `localization` prop directly.
  - `src/app/auth/redirect/page.tsx`: default post-auth landing (used when no `redirect_url` was carried over) — reads `publicMetadata.role` via `currentUser()` and redirects to `/admin` or `/catalogo` per `lib/clerk/redirects.ts::ROLE_HOME_ROUTE`; missing role defaults to `user`.
  - `src/app/api/webhooks/clerk/route.ts`: verifies the `user.created` webhook (`verifyWebhook` from `@clerk/nextjs/webhooks`, keyed by `CLERK_WEBHOOK_SIGNING_SECRET`) and sets `publicMetadata.role = "user"` on new sign-ups — the only place `role` is ever written server-side; no admin self-signup path exists.
  - `types/domain.ts` gained `Role = "admin" | "user"`, matching the existing `publicMetadata.role` convention from `architecture-context.md`.
  - `components/product/productBookingCard.tsx`'s "Iniciar conversación" CTA now links to `/sign-in?redirect_url=<current item path>` (via `usePathname()`) instead of a bare `/sign-in`, so the round trip back to the item page works once real gating exists.
  - `lib/supabase/client.ts` / `server.ts` already forward the Clerk session token with no `template` argument — correct for Clerk's native Supabase integration (no per-code-side template wiring needed); the `public_metadata` claim itself must still be added to the session token in the Clerk dashboard (see Next Up).
- `people` table (Supabase, not `profiles` — see auth+database wiring entry below for why) created via `supabase/migrations/0001_create_people.sql`: `id text PK` (= Clerk `sub`), `email`, `full_name`, `avatar_url`, timestamps. RLS: `people_select_own`/`people_update_own` (`auth.jwt() ->> 'sub' = id`) and `people_select_admin`/`people_update_admin` (`auth.jwt() -> 'public_metadata' ->> 'role' = 'admin'`); no insert/delete policy — only the webhook (service-role client, bypasses RLS) writes those.
- `lib/supabase/serviceRole.ts`: `createSupabaseServiceRoleClient()`, service/secret-key client for `app/api` webhook handlers only (never client-reachable). `app/api/webhooks/clerk/route.ts` upserts/deletes the matching `people` row on `user.created`/`user.updated`/`user.deleted`, in addition to defaulting `publicMetadata.role` on creation.
- Auth + database wiring pass (Clerk Third-Party Auth, `profiles` vs. `people`, route protection prefixes, mis-alquileres proof route):
  - **Table decision**: this task's spec called for a new `profiles` table (uuid PK + separate `clerk_user_id` linking column, `full_name`/`phone`/`company_name`). Since `people` already exists with `id text` = the Clerk user id directly, a second table would just duplicate the identity link and `full_name`. User confirmed: extend `people` instead — added `phone`/`company_name` via `supabase/migrations/0002_people_add_profile_fields.sql`, which also narrows the authenticated role's own UPDATE grant to just `full_name, phone, company_name` (column-level, on top of the existing row-level policy) so a user's own update path can't touch `email`/`avatar_url`/`id`/timestamps. The webhook (service-role, bypasses grants) maps `unsafe_metadata.phone`/`companyName` when present — not collected at sign-up today (no custom form fields per this task's "wiring, not custom UI" scope), so both stay `null` until a future profile-edit feature sets them.
  - **Route naming**: the task's spec named Spanish paths (`/panel-admin`, `/mis-conversaciones`, `/mis-alquileres`, `app/(auth)/iniciar-sesion`, `app/(auth)/crear-cuenta`). User confirmed: keep everything code-facing in English, consistent with code-standards.md and the earlier `/sign-in`/`/sign-up` precedent — `/admin` (unchanged), `/my-conversations`, `/my-rentals`; `/sign-in` and `/sign-up` moved into `app/(auth)/` (route grouping only, same URLs, same components) but keep their existing English folder names.
  - `proxy.ts` rewritten: `/admin`, `/my-conversations`, `/my-rentals` all require a session (redirect to `/sign-in?redirect_url=<original path>` if signed out); `/admin` additionally requires `sessionClaims.public_metadata.role === "admin"`, redirecting to `/` (not a 404) otherwise. Reads `sessionClaims` (from the JWT already in the request) rather than `auth.protect({ role })`'s default 404-on-mismatch behavior, since the spec wants a redirect for the wrong-role case specifically.
  - `types/globals.d.ts` (new): `CustomJwtSessionClaims.public_metadata.role` augmentation so `sessionClaims.public_metadata?.role` is typed — still requires the `public_metadata` custom claim configured in the Clerk Dashboard session token (see Next Up); this is the same claim RLS already reads, so no second differently-named claim is needed.
  - `lib/supabase/client.ts`/`server.ts` rewritten to the `accessToken: () => getToken()` pattern (native Third-Party Auth) instead of the previous manual `Authorization` header via a custom `fetch` override — functionally equivalent but is Supabase's documented API for this, and drops the now-unused `@supabase/ssr` dependency (no Supabase-managed cookie session exists; Clerk owns the session). Both now read `NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY` (Supabase's current key-naming, replacing `anon`); `serviceRole.ts` reads `SUPABASE_SECRET_KEY` (replacing `SUPABASE_SERVICE_ROLE_KEY`, which had been left unpopulated in `.env.local`).
  - `app/my-rentals/page.tsx` (new): minimal proof route (server component) — `auth()` for the Clerk `userId`, `createSupabaseServerClient()` to read that user's own `people` row under RLS, renders their name via `content/myRentals.ts`. Confirms the full chain (Clerk session → Third-Party Auth → RLS → `people`) end to end.
  - `.env.local` deduped (had drifted to duplicate/contradictory entries) and `.env.local.example` added (committed, empty values, `.gitignore` updated with `!.env.local.example` since the blanket `.env*` ignore would otherwise exclude it too). `CLERK_WEBHOOK_SIGNING_SECRET` renamed to `CLERK_WEBHOOK_SECRET` (matches this task's naming); the webhook route now passes it explicitly as `verifyWebhook(request, { signingSecret: ... })` since that's no longer the SDK's default-fallback env var name.
  - Fixed a latent bug surfaced by giving the Supabase clients a real `Database` generic (they were unparameterized before, so nothing type-checked `.from(...)` calls against the schema): `hooks/useConversation.ts` reads/writes a `messages` table that isn't migrated yet and casts snake_case rows into a camelCase `Message` type without transforming them — added a minimal `messages` entry to `types/supabase.ts` (documents the shape the hook already assumes; still not a real migration) and cast through `unknown` at the one call site that mixed the naming conventions. Conversation tables remain out of scope otherwise.
- Auth page visual pass per `context/designs/Auth-page/design_handoff_ingresar/README.md` (split-screen "Ingresar/Crear cuenta" design, high-fidelity):
  - `/sign-in` and `/sign-up` (now under `src/app/(auth)/sign-in/page.tsx`, `src/app/(auth)/sign-up/page.tsx` — see the auth+database wiring entry above) are plain (non-catch-all) routes rendering `components/auth/authShell.tsx`, since Clerk 7's `<SignIn/>`/`<SignUp/>` no longer support `routing="virtual"` (that value was dropped from the actual component prop type even though `RoutingStrategy` still lists it).
  - **Runtime bug found via a real browser (curl/SSR checks didn't catch it — this is a client-only Clerk validation)**: initially omitted `routing`/`path` entirely, which type-checked and rendered fine server-side, but threw `Clerk: The <SignIn/> component is not configured correctly` in the browser console because a non-catch-all route needs either `routing="hash"` or a `[[...rest]]` path. Fixed by passing `routing="hash"` explicitly to both `<SignIn/>` and `<SignUp/>` in `authPanel.tsx` — keeps the single-page tab-toggle behavior (no catch-all segments needed) while satisfying Clerk's internal routing check. Verified clean via a headless-Chrome/CDP console check, not just curl.
  - `components/auth/authShell.tsx`: two-column split screen (`min-[900px]:grid-cols-[...]`, photo panel hidden below 900px) — left column has the brand header + `TopoBackground` (new `variant="absolute"` prop scopes the existing fixed/viewport canvas to a panel instead) + `authPanel.tsx`; right column is `authPhotoPanel.tsx`.
  - `components/auth/authPanel.tsx` (client): our own tab toggle (`login`/`signup`, pill segmented control) mounts either `<SignIn/>` or `<SignUp/>` — clicking the tab or the bottom switch-prompt just flips local state, no navigation. Clerk's own built-in footer is hidden (`appearance.elements.footer: "hidden"`) since this custom switch prompt replaces it. Clerk's own `header`/title/subtitle (localized) are kept for each internal screen (start/password/forgotPassword/emailCode); our own "eyebrow" label sits above since Clerk has no eyebrow slot.
  - `components/auth/authPhotoPanel.tsx`: reuses `public/images/hero-anelo.png` (no separate Vaca Muerta photo asset exists) with the design's exact gradient overlays, pill badge, serif-italic heading accent, and stat row from `content/auth.ts`.
  - **Deliberate gap vs. the design**: the design's pre-submit "Contraseña / Código por email" segmented control (choosing a sign-in strategy *before* entering an email) isn't implemented — Clerk's `<SignIn/>` only offers a strategy choice *after* the identifier is submitted; doing it beforehand would require dropping to headless `useSignIn()`/`useSignUp()` hooks, which conflicts with `01-auth-page.md`'s explicit "use `<SignIn/>`/`<SignUp/>`, not custom flows" instruction. User confirmed: drop the toggle, keep the prebuilt components. Clerk's own post-submit method choice still works.
  - Also not implemented (Clerk has no themeable slot for it): the 52×52 icon badge (lock/mail) on the forgot-password/OTP screens.
- Profile page at `/profile` + session-aware navbar:
  - `app/(app)/` route group created (per architecture-context boundaries: authenticated user routes) with its own navbar+footer layout mirroring `(public)`'s; `my-rentals` moved into it (same URL) and `profile` added there.
  - `app/(app)/profile/page.tsx` (server component): requires a session (`auth()` → redirect to `/sign-in?redirect_url=/profile` if signed out; `/profile` also added to `proxy.ts` `AUTHENTICATED_PREFIXES`), reads the user's own `people` row under RLS, and renders three cards — user info (name, email, phone/company with "sin cargar" fallbacks, member-since date in `DD/MM/AAAA`), messages (static empty state + catalog CTA), and rentals (static empty state + `/my-rentals` link). Copy lives in `content/profile.ts`.
  - Navbar (`components/layout/navbar.tsx`) is now session-aware via Clerk's `<Show when="signed-in|signed-out">` — **Clerk 7 removed `SignedIn`/`SignedOut`; `<Show/>` is the replacement** — signed out shows only the "Ingresar" button; signed in shows the "Perfil" link (→ `/profile`, was a dead `/perfil` link) plus a "Cerrar sesión" `SignOutButton`, in both the desktop bar and the mobile sheet. New `nav.signOut` key in `content/site.ts`.
  - `server/people.ts::ensurePerson()` (new, `import "server-only"`): returns the signed-in user's `people` row, lazily creating it from the Clerk session when missing. Added because the profile page showed "No encontramos tu perfil" — sign-up alone never creates the row locally: the Clerk webhook is the creator, and it (a) isn't registered in the Clerk Dashboard yet and (b) can't reach localhost without a tunnel. User chose lazy self-heal + webhook over webhook-only-with-tunnel. **This relaxes the earlier "only the webhook writes `people` rows" rule**: `ensurePerson` uses the service-role client (RLS has no insert policy by design) but only ever writes data derived from the caller's own Clerk session — never caller input; the webhook remains the primary sync path in production (upsert keeps the two race-safe). `/profile` now reads through `ensurePerson()` instead of the RLS-scoped select; `/my-rentals` still exercises the RLS chain. `ensurePerson` logs (not swallows) select/upsert errors so future DB failures surface in server logs.
  - **Migrations actually applied to the remote Supabase DB** (previously written but never run — that was the real cause of "No encontramos tu perfil": `public.people` didn't exist). Applied `0001`/`0002` via `psql` (direct connection using `DB_PASSWORD` from `.env.local`), then found a second gap: `0001` never granted table privileges to the Supabase API roles (it relied on Supabase's default-privilege automation, which didn't fire for this table), so `service_role` got "permission denied". Added `supabase/migrations/0003_people_grants.sql` (`grant all … to service_role`, `grant select … to authenticated`) and applied it. Verified end to end: REST upsert as `service_role` → 201, delete → 204. Note for future tables created via raw SQL against this project: include explicit role grants in the migration, don't assume the default-privilege automation covers them.

- **Catalog data model in Supabase** (Fase 1, part 1/3 — schema only; `/catalogo` and `/edificios/[slug]` are NOT wired yet, that's parts 2–3). Seven new migrations (`0004`–`0010`), all applied to the remote DB via `psql`/`DB_PASSWORD` and verified:
  - `0004_create_categories` — `categories` (`item_type` check: estate|machinery|service, no "parking" — ParkingSpot is an ObjectItem subtype with a null category, not its own category).
  - `0005_create_buildings` — `buildings` with `floors_data` jsonb (kept jsonb, not normalized: the diagram also renders "external"/not-published unit slots that have no `items` row; published units instead link back via `items.building_id`, their live status derived from listings/rentals at read time). Shape documented in the migration and mirrored by `BuildingFloorsData` in `types/supabase.ts`.
  - `0006_create_items` — single-table `items` with `item_kind` discriminator (estate|machinery|service|parking_spot) and per-subtype nullable columns (no table-per-subtype inheritance). Estate carries `latitude`/`longitude` for MapLibre. `category_id`/`building_id` nullable FKs.
  - `0007_create_listings` — `listings` 1:1 with items (`item_id` unique), `admin_id` → `people`. Also adds the `items_select_published` policy here (deferred from `0006` since it references `listings`): an item is world-readable exactly when a published listing points at it.
  - `0008_create_availability` — `availability`; public read scoped the same way (only items with a published listing).
  - `0009_create_rentals` — **minimal** `rentals` only (id, user_id, status=RentalStatus, dates, total_amount) so `rental_items` has a target; full payments/reviews model is Fase 2/5. Owner-or-admin RLS, not public.
  - `0010_create_rental_items` — join table so one Rental can bundle multiple Items; visibility follows the parent rental (owner or admin), no public read.
  - RLS enabled on all 7; writes to items/listings/buildings/availability/categories are admin-only via `auth.jwt() -> 'public_metadata' ->> 'role'`; explicit `service_role`/`authenticated`/`anon` grants in every migration (Supabase's default-privilege automation doesn't fire for raw-SQL tables — same gotcha as `0003_people_grants`). anon gets SELECT only on the public tables, nothing on rentals/rental_items.
  - Verified end to end: in-DB JWT simulation (anon/owner/other-user/admin) all matched expectations, plus real anonymous REST calls (categories/buildings/items → 200; rentals read, buildings/categories insert → 401). All seed data used for verification was inside a rolled-back transaction — none persisted.
  - `types/supabase.ts` extended with all seven tables (Row/Insert/Update/Relationships) + enum/jsonb helper types and row aliases (`CategoryRow` — not `Category`, to avoid clashing with the `Category` string union in `domain.ts`). `npm run build` passes.
- **Responsive search filters and scroll-aware brand logo at `/catalogo`**:
  - Replaced the horizontal scrolling filter buttons on mobile/tablet viewports with a single "Filtros" button.
  - Clicking "Filtros" opens a custom slide-out popup `<Sheet>` (`side="right"`) containing all filters: Categories, Price Range inputs, Contract Duration, and Availability.
  - Added a visual active badge to the mobile "Filtros" button when filters are active, and a "Limpiar todo" reset link.
  - On desktop (`md` and up), the inline filters remain visible and use `flex-nowrap` to prevent wrapping.
  - Passed `hideText` prop to `<BrandMark />` so that when `showCompactPill` is true (scrolled past threshold on `/catalogo`), the "MILOCATIVA" brand text hides only on mobile viewports (below `md`), leaving only the icon to free space for the search bar, while remaining visible on desktop.
  - Applied subtle red tones to both the desktop (`text-error/80 hover:text-error`) and mobile (`variant="destructive"`) versions of the "Cerrar sesión" button.
  - Added `scroll-ml-4 sm:scroll-ml-0` to the building snap carousel cards (`BuildingCard` and `ShowAllCard` in `buildingsRail.tsx`) so they snap exactly 16px (1rem) from the screen edge on mobile, ensuring proper margins on scroll.

- **`/profile` redesigned as a member dashboard (from the AIDesigner "page 3" mock) + header user cluster**:
  - `app/(app)/profile/page.tsx` rebuilt (still a server component; same `auth()` + `ensurePerson()` data path) into a two-column layout: a left profile card (avatar from `person.avatar_url ?? user.imageUrl` via plain `<img>` — Clerk/Supabase are remote hosts, avoids adding `next.config` `remotePatterns`; name, `company_name` subtitle, static "Usuario Verificado" pill, email/phone contact rows, `EditProfileButton`) and a right main column (greeting, three stat cards, `Mis Alquileres/Historial/Pagos` tabs). Left card is `lg:sticky`; single-column and stat cards go 1-up on mobile.
  - **Design-only scope (user-confirmed)**: no new rentals query and no fabricated rows. `RentalsTabs` receives `rentals={[]}`, stat counts are `0`, and each tab shows its empty state. The layout is fully wired to render real data once a rentals feature exists.
  - New `components/profile/`: `rentalCard.tsx` (presentational `RentalCard` + `RentalCardData` — status/category tags reuse `RENTAL_STATUS_COLORS`/`CATEGORY_COLORS` + `catalogContent.categoryTags`; status-dependent action buttons are visual placeholders until rentals/chat exist), `rentalsTabs.tsx` (`"use client"` tab switch, filters rentals per tab), `editProfileButton.tsx` (`"use client"`, opens Clerk's `openUserProfile()` account modal — no new edit route/backend).
  - `content/profile.ts` extended with greeting/subtitle, contact/verified/edit labels, stat labels, tab labels + per-tab empty copy, rental-card action labels, and `rentalStatusLabels` (Spanish `RentalStatus` display strings). Existing keys kept.
  - Header (`components/layout/navbar.tsx`): signed-in users now get the dashboard cluster via new `components/layout/userMenu.tsx` (`"use client"`) — a notification **bell (visual placeholder, static unread dot, no backend)** + an avatar pill (`useUser().imageUrl`) that opens an account `Sheet` (nav links + Perfil + Cerrar sesión) on every breakpoint, replacing the old text "Perfil"/"Cerrar sesión" desktop links and the signed-in branch of the mobile hamburger sheet. Signed-out header (Ingresar + hamburger sheet) unchanged. New `nav.notifications`/`nav.accountMenu` aria-label keys in `content/site.ts`.
  - Typecheck + ESLint clean on all touched files.

## In Progress

## Next Up

- Wire `/catalogo` and `/edificios/[slug]` to Supabase: listings, buildings, per-unit rental statuses; replace in-memory favorites with real user state.
- Price / Duración / Disponibilidad filter dropdowns and the search input are visual stubs — define their behavior and implement.
- `/profile`'s messages/rentals sections are static empty states — wire them to real conversations/rentals once those tables exist.
- `NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY`/`CLERK_SECRET_KEY` and the Supabase keys are now real (set in `.env.local`, not committed). Still needed in the respective dashboards, not code: (1) a `user.created`/`user.updated`/`user.deleted` webhook endpoint pointing at `/api/webhooks/clerk` in the Clerk dashboard, signing secret copied into `CLERK_WEBHOOK_SECRET`; (2) a `public_metadata` custom claim (`{{user.public_metadata}}`) added to the Clerk session token, so both `sessionClaims.public_metadata.role` (proxy.ts) and `auth.jwt() -> 'public_metadata' ->> 'role'` (RLS) resolve; (3) Supabase project → Authentication → Third-Party Auth → Clerk integration enabled, pointing at the Clerk instance domain (no JWT template, no shared secret).
- Gate the actual "start a conversation" / "request a rental" actions (not built yet) behind an auth check once those features exist — today only the product page's "Iniciar conversación" CTA sends unauthenticated visitors through `/sign-in?redirect_url=...`.
- `app/my-conversations` doesn't exist yet (only the `proxy.ts` protection prefix is wired) — add the page once chat is built.

## Open Questions

- Footer links to `/contacto`, `/terminos`, and `/privacidad` — these pages are not defined in the context files yet.
- Brand casing: the v0 design brief uses "MILOCATIVA" while `project-overview.md` says "Mi Locativa" — the landing page follows the design brief ("MILOCATIVA").
- Hero trust stats ("+120 activos disponibles") are filler numbers from the design brief; replace with real counts once the catalog is live.
- Only Torre Añelo has building detail data, so every building card links to `/edificios/torre-anelo` for now (the design prototype does the same); real slugs need per-building unit data from Supabase.
- Product detail pages now exist at `/catalogo/[id]` for catalog products; building unit blocks (`buildingDiagram.tsx`) still link to `#` since building units aren't in the catalog product id space yet.
- Route naming for auth/protected pages has come up twice now with conflicting Spanish-path suggestions from feature specs (`/iniciar-sesion`, `/registrarse`, `/panel-admin`, `/mis-conversaciones`, `/mis-alquileres`) — both times the user confirmed English wins (`/sign-in`, `/sign-up`, `/admin`, `/my-conversations`, `/my-rentals`), consistent with code-standards.md's "code identifiers stay in English" and the routes/links already established. Treat this as the standing convention rather than re-asking per feature.
- **Notifications system doesn't exist.** The `/profile` header bell (`components/layout/userMenu.tsx`) and the "Mensajes Nuevos" stat card are visual placeholders (bell has a static unread dot; message count is hardcoded `0`). No notifications/messages table, no unread count, and the bell has no action yet — wire these once a notifications/chat feature lands.
- **`people` has no user-title or verification field.** The design's left-card subtitle ("Operaciones – Techint") and "Usuario Verificado" pill don't map to real columns: the subtitle falls back to `company_name` and the verified pill is a static design element shown for everyone. The mock's location row is dropped entirely (no field). Add columns (e.g. `job_title`, `verified`) + a profile-edit surface if these should become real.

## Architecture Decisions

- Tailwind v4 CSS-first config (no tailwind.config.js) — all theme tokens live in globals.css via @theme inline.
- shadcn/ui components live in components/ui/ and must not be modified after installation.
- Light (default) + dark theme: petrol-blue (#03597F) primary accent, teal and sky secondary accents. Dark theme uses `html[data-theme="dark"]` attribute overrides (not a `.dark` class) — see ui-context.md "Theme switching".
- Next.js 16 uses proxy.ts (not middleware.ts) — the middleware file convention was renamed in v16.
- No Prisma/ORM: Supabase Postgres accessed directly via RLS-scoped Supabase clients (browser and server), authorized via Clerk Third-Party Auth (`accessToken` callback forwarding the native Clerk session token — no JWT template, no shared secret).
- Role (admin | user) lives in Clerk publicMetadata, not a database table; RLS policies read it via auth.jwt() -> 'public_metadata' ->> 'role'.
- File storage (item photos, documents) uses Supabase Storage, not Vercel Blob.
- Realtime chat and in-app notifications use Supabase Realtime, not a third-party collaboration service.
- Payments: Stripe and MercadoPago, reconciled via verified webhooks in app/api; payment status is never set client-side.
- Maps: MapLibre GL JS + open vector tiles (not Google Maps / Mapbox) — custom "keen style" achieved by editing the vector style JSON directly with project design tokens, no per-request cost.
- ParkingSpot (cochera) is a new ObjectItem subtype — rentable standalone or bundled with an Estate in the same Rental.
- Building groups Estates and ParkingSpots via an optional buildingId (nullable — standalone properties don't need a Building record).
- Rental no longer maps 1:1 to a single Item — bundled rentals go through a rental_items join table; always read/write through it, never through a single itemId on Rental.
- Estate carries latitude/longitude, captured at listing creation/edit time, as the source of truth for map markers.
- `proxy.ts` must live at `src/proxy.ts` (not the repo root) because the project uses a `src/` layout — this is a Next.js 16 file-location rule, distinct from the middleware.ts→proxy.ts rename already noted below.
- New sign-ups get `role: "user"` written server-side by a Clerk `user.created` webhook (`app/api/webhooks/clerk`), not client-side — `publicMetadata` can't be set from the client.

## Session Notes

- Project uses Next.js 16, React 19, Tailwind v4, TypeScript.
- Light (default) + dark theme, toggled via the navbar moon/sun button (`components/layout/themeToggle.tsx`, `hooks/useTheme.ts`), persisted in `localStorage` (`milocativa-theme`). All color tokens are CSS custom properties mapped via @theme inline, defined on `:root` (light) and overridden on `html[data-theme="dark"]`.
- Tailwind utility classes: bg-base, bg-surface, bg-elevated, bg-subtle, text-copy-primary, text-copy-muted, border-surface-border, text-brand, bg-accent-dim, text-accent-teal, etc.
- @clerk/nextjs (Core 3, requires Next.js 15.2.3+) installed; auth file is proxy.ts, not middleware.ts.
- createRouteMatcher() is deprecated in this Clerk version — route protection uses inline pathname.startsWith() checks; the admin role check reads `sessionClaims.public_metadata.role` directly (not `auth.protect({ role })`, since the spec wants a redirect-to-home on role mismatch rather than `auth.protect`'s default 404).
- Clerk publicMetadata.role drives both the proxy.ts route gate and the Supabase RLS policies — keep both in sync per any role logic change.
- UI copy is entirely in Spanish, sourced from content/dictionary files; code identifiers are entirely in English camelCase.
